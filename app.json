{
  "name": "cover-letter-generator",
  "description": "Backend for cover letter generator",
  "scripts": {
    "postdeploy": "npm install --prefix server && npm start --prefix server"
  },
  "env": {
    "NODE_ENV": "production"
  }
}
